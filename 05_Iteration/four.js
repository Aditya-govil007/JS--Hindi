const myobject={
    js : 'javascript',
    cpp: 'c++',
    py : 'python',
    swift:'swift by apple'
}

// -----------------for in loop-------------
for (const key in myobject) {
    // console.log(`${key} shortcut is for ${myobject[key]}`);
    

}

const progrming = ['js','py','cpp']
for (const key in progrming) {
    console.log(`${progrming[key]}`);
    
    
    
}